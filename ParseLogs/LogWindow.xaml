<Window x:Name="window" x:Class="ParseLogs.LogWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:ParseLogs"
        xmlns:dj="clr-namespace:DJ;assembly=NLogViewer"
        xmlns:system="clr-namespace:System;assembly=mscorlib"
        mc:Ignorable="d"
        FontSize="18"
        Title="LogWindow" Height="450" Width="800">
    <Window.Resources>
        <DataTemplate x:Key="levelButtonTemplate">
            <Button Content="{Binding Path=.}"></Button>
        </DataTemplate>
    </Window.Resources>
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"></RowDefinition>
            <RowDefinition Height="*"></RowDefinition>
        </Grid.RowDefinitions>
        <StackPanel Orientation="Horizontal">
            <ItemsControl ItemTemplate="{StaticResource levelButtonTemplate}" ItemsSource="{Binding Source={x:Static local:LogWindow.LoggingLevels}}">
            </ItemsControl>

        </StackPanel>
        <dj:NLogViewer Name="LogViewer" Grid.Row="1" Grid.Column="0"></dj:NLogViewer>
    </Grid>
</Window>
